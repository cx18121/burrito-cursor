import XCTest

// Placeholder so SPM accepts the test target. Removed once ConfigTests.swift lands in Task 2.
final class PlaceholderTests: XCTestCase {
    func testPlaceholder() {
        XCTAssertTrue(true)
    }
}
